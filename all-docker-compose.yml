services:
  postgres:
    image: postgres:18-alpine
    container_name: postgres
    restart: always
    volumes:
      - ./postgres:/var/lib/postgresql
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "avdb"
      # 以系统为准，下面是绿联的pgid和puid
      PGID: "10"
      PUID: "1000"
      TZ: "Europe/Rome"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 4G
    networks:
      - avdb_net
      
  avdb:
    image: leolitaly/avdb:latest
    container_name: avdb
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - 8000:8000
    volumes:
      - ./avdb:/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      # 以系统为准，下面是绿联的pgid和puid
      PGID: "10"
      PUID: "1000"
      # 时区，中国改成 Asia/Shanghai
      TZ: "Europe/Rome"
      # 数据库地址
      DATABASE_URL: "postgresql://postgres:postgres@postgres:5432/avdb"
      # 自动更新端地址，保持默认即可
    mem_limit: 4G
    networks:
      - avdb_net

  trawl:
    image: ghcr.io/germondai/trawl:latest
    container_name: trawl
    restart: always
    ports:
      - "${PORT:-8191}:8191"
    shm_size: 1gb
    mem_limit: 2g
    environment:
      BROWSER_POOL_SIZE: "1"
      REDIS_URL: "redis://redis:6379"
    depends_on:
      - redis
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://trawl:8191/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s
    networks:
      - avdb_net
      
  redis:
    image: redis:8.8-alpine
    container_name: redis
    restart: always
    mem_limit: 1g
    volumes:
      - ./redis:/data
    networks:
      - avdb_net

networks:
  avdb_net:
    external: true
